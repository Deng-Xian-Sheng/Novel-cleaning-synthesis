# 小说 AI 助手项目进度

## 项目概述
通过 https://chat.dphn.ai API + Milvus + Qwen3-Embedding-0.6B + OpenWebUI 实现一个写小说的 AI 助手。

---

## 当前状态
**最后更新**: 2026-03-25

### ✅ 已完成

#### 1. 基础设施准备
- [x] Milvus 部署（端口 19530）
- [x] Qwen3-Embedding-0.6B 部署（llama.cpp，端口 8081）
- [x] Milvus 配置优化（requestTimeout: 30000ms）

#### 2. 小说向量化脚本
- [x] `index_novels.py` 编写完成
  - 支持 4 类内容向量化：user_request、novel_summary、block_summary、block_content
  - 支持无 block 的小说结构（直接读取正文.md）
  - 逐条向量化（CPU 友好）
  - 每 10 条批量插入 Milvus
  - 断点续传（index_progress.json）
- [x] `verify_and_clean_progress.py` 校验脚本编写完成
  - 可校验 progress 和 Milvus 数据一致性
  - 自动清理无效记录

#### 3. 数据验证
- [x] Collection 结构确认：`novel_chunks`
- [x] 数据完整性验证通过（无丢失）

#### 4. 技术方案确定
- [x] 预检索 + 提示词方案确定
- [x] 提示词设计完成（见下方"小说生成提示词"）
- [x] openai_proxy_prod.py 行为确认（无工具时不添加工具提示词）

#### 1. 小说索引
- [x] 运行 `index_novels.py` 完成全部小说向量化
  - 总小说数：900+
  - 预计时间：数小时到十几小时（CPU 模式）

---

### 2026-03-24 六次修复
- 问题：检索结果仍包含 novel_summary、block_summary 类型的 chunk
  - 原因：之前的 filter `'chunk_type != "user_request"'` 只排除了 user_request，仍返回 summary 类型
  - 修复：修改 filter 为 `'chunk_type == "block_content"'`，只查询正文内容
  - 现在：查询结果只包含 block_content 类型（段落正文），排除 user_request、novel_summary、block_summary
  - 文件：`/workspace/open-webui/backend/open_webui/novel_rag.py`

### 2026-03-24 五次修复
- 问题1：多轮对话时查询不准确（如第二轮用户说"继续"）
  - 原因：只使用最后一条用户消息进行 Milvus 查询，缺乏上下文
  - 修复：新增 `_extract_user_queries()` 函数，收集所有历史用户消息（排除之前注入的 `<references>`），组合后作为查询
  - 现在：多轮对话时查询会携带完整上下文，提高检索准确性

- 问题2：检索结果包含 user_request 类型的 chunk
  - 原因：Milvus 查询没有过滤 chunk_type
  - 修复：在 `search()` 函数中添加 `filter='chunk_type != "user_request"'`
  - 现在：查询结果只包含 novel_summary、block_summary、block_content 类型，排除用户要求

### 🔄 进行中/待开始

#### 2. OpenWebUI 对接（已完成，已修复系统提示词问题）

**2026-03-24 四次修复**：
- 问题：日志中出现 `Found 10 relevant novel chunks for query: ### Task: Suggest 3-5 relevant follow-up questions...`
  - 原因：OpenWebUI 内部任务（如建议后续问题、生成标题等）使用了 `### Task:` 开头的提示词，这些不是用户真实输入，但 RAG 模块也对它们进行了小说搜索
  - 修复：在 `process_novel_rag` 函数中添加检查，如果查询以 `### Task:` 开头则跳过 RAG 搜索
  - 现在：内部任务不会触发无意义的小说检索，只有真正的用户输入才会进行 RAG

**2026-03-24 修复**：
- 问题1：模型没有按照提示词格式输出，怀疑提示词未生效
  - 原因：代码只注入 `<references>` 到用户消息，但没有自动注入**系统提示词**
  - 修复：在 `novel_rag.py` 中添加 `SYSTEM_PROMPT` 常量，并在 `process_novel_rag` 中调用 `_add_system_prompt` 函数自动注入系统提示词
  - 现在：每次请求都会自动添加系统提示词，无需在 OpenWebUI 模型配置中手动设置

- 问题2：前端没有显示 Milvus 查询结果（sources）
  - 原因：代码只将 references 注入到用户消息给 LLM 看，但没有返回 sources 给前端显示
  - 修复：
    1. 在 `novel_rag.py` 中添加 `format_sources()` 方法，将搜索结果格式化为前端 Citations 组件期望的格式
    2. 修改 `process_novel_rag()` 返回字典包含 `messages` 和 `sources`
    3. 在 `chat.py` 中捕获 sources 并在响应中返回（支持流式和非流式响应）
  - 现在：前端会显示检索到的小说来源，用户可以点击查看详情

**2026-03-24 二次修复**：
- 问题：前端仍然没有显示 Milvus 查询结果
  - 原因分析：OpenWebUI 前端通过 **WebSocket 事件** 接收 sources，而不是从 HTTP 流式响应中解析
  - 修复：
    1. 在 `chat.py` 中使用 `get_event_emitter(metadata)` 获取事件发射器
    2. 对每个 source 调用 `event_emitter({"type": "source", "data": source})` 通过 WebSocket 发送
    3. 前端 `chatEventHandler` 收到 `source` 类型事件后，自动将 source 添加到 `message.sources`
    4. 简化代码，删除之前通过流式响应发送 sources 的逻辑（已无用）
  - 关键代码：
    ```python
    if novel_rag_sources:
        metadata = form_data.get("metadata", {})
        if metadata.get("chat_id") and metadata.get("message_id") and metadata.get("user_id"):
            event_emitter = get_event_emitter(metadata)
            for source in novel_rag_sources:
                await event_emitter({"type": "source", "data": source})
    ```

**2026-03-24 三次修复**：
- 问题：多轮对话时系统提示词重复添加
  - 原因分析：`_add_system_prompt` 函数每次请求都会检查并添加系统提示词，但多轮对话时 messages 包含历史记录，函数发现已有 system 消息后会在其内容后面**追加**提示词，导致重复累积
  - 第一轮: `[system(我们的提示词), user+refs, assistant]`
  - 第二轮: `[system(我们的提示词+我们的提示词), user+refs, assistant, user+refs, assistant]`
  - 修复：
    1. 在 `SYSTEM_PROMPT` 开头添加唯一标记 `SYSTEM_PROMPT_MARKER = "[NOVEL_RAG_SYSTEM_PROMPT_V1]"`
    2. 修改 `_add_system_prompt` 函数逻辑：
       - 检查 system 消息是否已包含标记 → 包含则直接返回（跳过）
       - 不包含标记 → 追加到现有 system 消息
       - 没有 system 消息 → 在开头添加
  - 现在：多轮对话时系统提示词只会添加一次，不会重复
- [x] Clone OpenWebUI 仓库
- [x] 分析需要修改的部分
- [x] 实现与 Milvus 的搜索对接
- [x] 实现与 https://chat.dphn.ai 的 API 对接

**修改内容：**
1. 创建 `backend/open_webui/novel_rag.py` - 小说RAG核心模块
   - 调用 llama.cpp embedding 服务进行向量化
   - 连接 Milvus 进行相似度搜索
   - 按提示词格式组装 `<references>` XML
   
2. 修改 `backend/open_webui/config.py` - 添加配置项
   - `NOVEL_RAG_ENABLED` - 启用/禁用小说RAG
   - `NOVEL_RAG_EMBEDDING_URL` - llama.cpp服务地址
   - `NOVEL_RAG_MILVUS_HOST/PORT` - Milvus连接信息
   - `NOVEL_RAG_COLLECTION_NAME` - Collection名称
   - `NOVEL_RAG_TOP_K` - 返回结果数量
   - `NOVEL_RAG_SCORE_THRESHOLD` - 相似度阈值

3. 修改 `backend/open_webui/utils/chat.py` - 集成RAG逻辑
   - 在 `generate_chat_completion` 中调用 `process_novel_rag`
   - 自动将检索结果注入用户消息

4. 修改 `backend/open_webui/main.py` - 初始化配置
   - 导入并初始化 novel_rag 相关配置

#### 3. 搜索逻辑设计（已完成）
- [x] 用户查询向量化（通过 llama.cpp:8081）
- [x] Milvus 相似度搜索（端口 19530）
- [x] 结果排序和过滤（按相似度分数）
- [x] 提示词组装（XML格式 `<references>`）

---

## 技术方案

### 整体架构
```
┌─────────────────────────────────────────────────────────────────┐
│  OpenWebUI (前端)                                               │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                              │
│  ├─ 接收用户输入                                                │
│  ├─ 查询向量化 (llama.cpp:8081)                                │
│  ├─ Milvus 搜索 (端口 19530)                                    │
│  ├─ 组装提示词                                                  │
│  └─ 请求 openai_proxy_prod.py:8000                             │
└────────────────┬────────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│  openai_proxy_prod.py (容器内)                                 │
│  └─ 转发到 https://chat.dphn.ai/api/chat                       │
└─────────────────────────────────────────────────────────────────┘
```

OpenWebUI前端需要用UI组件很好的显示RAG搜索到的内容

### 数据流向
```
用户要求 → 向量化(Qwen3-Embedding) → Milvus 搜索 → 获取相关内容 → 
组装提示词 → openai_proxy_prod.py:8000 → chat.dphn.ai API → 返回小说文本
```

### Milvus Collection 结构
```yaml
Collection: novel_chunks
Fields:
  - id: VARCHAR(128) primary_key      # {novel_id}:{chunk_type}:{block_id}
  - novel_id: VARCHAR(64)             # 小说目录名
  - chunk_type: VARCHAR(32)           # user_request/novel_summary/block_summary/block_content
  - block_id: VARCHAR(128)            # block1/block2/中文段落名
  - block_title: VARCHAR(512)         # 段落标题
  - content: VARCHAR(16384)           # 原始 markdown 内容
  - embedding: FLOAT_VECTOR(1024)     # Qwen3-Embedding-0.6B 向量
```

### 向量化策略
- **Query 类型**（用户查询）：添加 instruction 后向量化
  ```
  Instruct: Given a web search query, retrieve relevant passages that answer the query
  Query: {用户查询内容}
  ```
- **Document 类型**（小说内容）：直接向量化，不加 instruction

### openai_proxy_prod.py 行为
- ✅ 无 tools 或 tool_choice == "none" 时：不添加任何工具提示词
- ✅ 有 tools 且 tool_choice != "none" 时：添加工具调用提示词
- 这符合当前需求（暂时不用工具调用，但保留未来能力）

---

## 小说生成提示词

### 系统提示词

```txt
你是一位小说写作助手。

系统会根据用户要求从小说库中检索参考片段，请基于用户要求和参考片段，创作一篇原创小说。

用户要求和参考片段在用户消息中。

## 用户消息格式说明

用户消息包含两部分：

1. **<references> 标签之前**：用户当前对你说的话（写作要求、修改意见、继续指令等）

2. **<references> 标签之内**：从小说库检索到的参考片段，可能包含以下类型：
   - `<user_request source="小说目录名">`：生成该小说时的原始用户要求
   - `<novel_summary source="小说目录名">`：整篇小说的总体概述
   - `<block_summary source="小说目录名" block="段落ID">`：某一段落的摘要
   - `<block_content source="小说目录名" block="段落ID">`：段落的具体正文内容

## 一、可借鉴的内容（方法层面）

你可以借鉴参考片段中的以下方法，但不能复制具体内容：

- **开篇切入方式**：如何吸引读者进入故事
- **信息释放顺序**：如何安排情节的揭示节奏
- **人物冲突组织方式**：如何设置和推进矛盾
- **情绪递进方式**：如何层层推进情感
- **段落长度与节奏**：如何控制叙事速度
- **高潮与收束方式**：如何处理故事的高潮和结尾
- **对话与描写比例**：如何平衡对话和场景描写

## 二、写作原则

### 1. 先保证"像小说"，再追求"像参考"
你的最终产出必须首先是一个完整、流畅、有吸引力的故事，而不是参考拼贴。

### 2. 开头必须尽快建立吸引力
开篇应尽快给出以下至少一项：
- 异常事件
- 强冲突
- 高风险问题
- 强烈情绪
- 令人好奇的信息缺口

### 3. 人物必须具备可区分性
至少让主要角色在以下方面有所区别：
- 说话方式
- 目标
- 恐惧
- 情绪表达方式
- 行动习惯

### 4. 冲突要逐步升级
不要从头到尾同一强度。要有：
- 引子
- 加压
- 反弹
- 升级
- 爆发
- 余波/收束

### 5. 不要只有设定，没有场景
尽量通过具体场景、动作、对话、细节来呈现，而不是用大段说明代替故事。

### 6. 语言风格要稳定
根据用户要求选择合适风格：
- **网文感**：节奏更快、钩子更明显、段落更利落
- **文艺细腻**：描写更精确、情绪递进更柔和
- **影视感**：画面、动作、转场更鲜明
- **悬疑感**：信息控制更严格，悬念层层递进

### 7. 结尾要完成承诺
结尾至少要回应开头提出的核心问题、核心情绪或核心冲突。

## 三、原创性要求

**可以借鉴**：结构、节奏、写法、桥段组织方式

**绝对禁止**：
- 直接照抄句子
- 只改几个词就复用
- 复制具体设定组合到足以构成雷同
- 把多个参考文本拼接成新文

## 四、输出格式

默认按以下格式输出：

### 参考说明
- 借鉴点：（简述你从参考片段中学到的方法）

### 写作方案
- 题材与基调：
- 核心冲突：
- 开篇切入：
- 中段推进：
- 高潮设计：
- 结尾方式：
- 文风控制：

### 正文
（直接开始写小说，不要加额外说明）

如果用户只想直接看结果，可以把"参考说明"和"写作方案"压缩得很简短（3~5行），但你仍然必须在内部完成规划。

## 五、禁止事项

- 禁止忽略用户要求，写成其他类型
- 禁止为了模仿参考而导致人物前后不一致、剧情断裂
- 禁止照抄、近似改写、拼接参考文本
- 禁止偏离用户要求自说自话

## 六、多轮对话说明

如果用户提出修改意见，请：
1. 理解用户的具体修改要求
2. 参考原始片段和之前的写作方案
3. 在保持整体一致性的前提下进行修改
4. 输出修改后的内容

如果用户要求继续写下一部分，请：
1. 回顾已写内容的脉络
2. 保持人物设定和文风的一致性
3. 按照冲突升级的节奏继续推进
4. 确保与已写内容衔接自然
```

### User Message 格式示例

```
我想写一本修仙小说，主角是废柴逆袭

<references>
  <user_request source="novel_001">请写一篇修仙小说，主角从废柴开始...</user_request>
  <novel_summary source="novel_001">山村少年李二狗被测出废灵根...</novel_summary>
  <block_summary source="novel_002" block="block1">主角参加宗门测试，被众人嘲笑...</block_summary>
  <block_content source="novel_003" block="block2">"凭什么？"少年握紧拳头，指甲嵌入掌心...</block_content>
</references>
```

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `/workspace/index_novels.py` | 小说向量化主脚本 |
| `/workspace/verify_and_clean_progress.py` | 数据校验脚本 |
| `/workspace/index_progress.json` | 进度记录（运行时生成） |
| `/workspace/openai_proxy_prod.py` | chat.dphn.ai 的 OpenAI 兼容代理 |
| `/workspace/open-webui/backend/open_webui/novel_rag.py` | 小说RAG核心模块 |
| `/workspace/open-webui/backend/open_webui/config.py` | 添加Novel RAG配置 |
| `/workspace/open-webui/backend/open_webui/utils/chat.py` | 集成RAG逻辑 |
| `/workspace/open-webui/backend/open_webui/main.py` | 初始化RAG配置 |

---

## 启动说明

### 前置条件

确保以下服务已就绪：
- ✅ Milvus 运行在端口 19530
- ✅ Qwen3-Embedding (llama.cpp) 运行在端口 8081
- ✅ 小说数据已完成向量化（运行过 `index_novels.py`）

### 1. 启动依赖服务

#### 1.1 启动 Milvus（容器外）
```bash
cd /path/to/milvus/docker-compose
docker-compose up -d

# 验证
curl http://localhost:19530
```

#### 1.2 启动 Qwen3-Embedding（llama.cpp，容器外）
```bash
./llama-b7770/llama-server \
  -hf Qwen/Qwen3-Embedding-0.6B-GGUF:Q8_0 \
  --embedding --pooling last \
  -ub 8192 --verbose-prompt \
  --port 8081

# 验证
curl http://localhost:8081/v1/embeddings
```

#### 1.3 启动 openai_proxy_prod.py（容器内）
```bash
cd /workspace
python openai_proxy_prod.py

# 验证
curl http://localhost:8001
```

### 2. 启动 OpenWebUI（容器内）

#### 2.1 安装依赖（首次）
```bash
cd /workspace/open-webui/backend
pip install -r requirements.txt
```

#### 2.2 构建前端（首次或前端代码更新后）
```bash
cd /workspace/open-webui

# 安装前端依赖
npm install

# 构建前端
npm run build
```

#### 2.3 设置环境变量
```bash
# 小说 RAG 配置（必须）
export NOVEL_RAG_ENABLED=true
export LLAMA_EMBEDDING_URL=http://localhost:8081/v1/embeddings
export MILVUS_HOST=localhost
export MILVUS_PORT=19530
export NOVEL_COLLECTION_NAME=novel_chunks
export NOVEL_RAG_TOP_K=10
export NOVEL_RAG_SCORE_THRESHOLD=0.5

# OpenWebUI 基础配置
export WEBUI_SECRET_KEY=your-secret-key-here
export PORT=8082

# OpenAI API 配置（指向 openai_proxy_prod.py）
export OPENAI_API_BASE_URL=http://localhost:8001/v1
export OPENAI_API_KEY=sk-dummy-key
export ENABLE_OPENAI_API=true

# 禁用自带的上传文档（RAG）功能
export RAG_EMBEDDING_MODEL=""
export BYPASS_EMBEDDING_AND_RETRIEVAL=true
```

#### 2.4 启动服务

**方式一：使用 start.sh 脚本（推荐）**
```bash
cd /workspace/open-webui/backend
bash start.sh
```

**方式二：直接使用 uvicorn**
```bash
cd /workspace/open-webui/backend
python -m uvicorn open_webui.main:app --host 0.0.0.0 --port 8082 --workers 1
```

**方式三：后台运行**
```bash
cd /workspace/open-webui/backend
nohup bash start.sh > /workspace/openwebui.log 2>&1 &

# 查看日志
tail -f /workspace/openwebui.log
```

#### 2.5 验证启动
```bash
# 检查服务是否启动
curl http://localhost:8080/health

# 访问 Web 界面
# http://localhost:8080
```

### 3. 配置 OpenWebUI（首次使用）

1. **访问 Web 界面**
   - 打开浏览器访问 http://localhost:8080
   - 注册/登录管理员账号

2. **配置模型连接**
   - 进入 **Admin Panel > Settings > Connections**
   - 点击 **+** 添加 OpenAI API 连接：
     - **URL**: `http://localhost:8001`
     - **Key**: 任意值（如 `sk-dummy`，不会被实际使用）
   - 保存设置

3. **验证模型列表**
   - 返回首页，检查模型列表中是否出现配置的模型
   - 如果没有，点击刷新按钮

### 4. 使用小说助手

1. **创建新对话**
   - 点击 "New Chat"
   - 选择配置的模型（如 gpt-4o-mini）

2. **输入写作要求**
   - 例如："我想写一本修仙小说，主角是废柴逆袭"
   - 或："写一个悬疑故事，开头要有意外发现"

3. **系统自动处理**
   - 向量化用户查询
   - 搜索 Milvus 中的相关小说
   - 将结果组装成 `<references>` XML 格式
   - 注入到提示词中
   - 发送到 chat.dphn.ai 生成小说

4. **查看结果**
   - 模型会返回包含 "参考说明"、"写作方案"、"正文" 的完整回复
   - 可以继续对话要求修改或续写

### 5. 常见问题排查

| 问题 | 排查方法 |
|------|---------|
| 无法访问 8080 端口 | 检查 `PORT` 环境变量，确认端口未被占用 |
| 模型列表为空 | 检查 openai_proxy_prod.py 是否运行，查看后端日志 |
| RAG 未生效 | 检查 `NOVEL_RAG_ENABLED=true` 是否设置，查看日志中是否有 "Novel RAG processed" |
| 检索不到内容 | 检查 Milvus 是否有数据，检查 embedding 服务是否正常 |
| 生成内容质量差 | 检查提示词是否正确注入，查看日志中的 messages 内容 |

---

## 注意事项

1. **etcd 配置**：不要修改 ETCD_HEARTBEAT_INTERVAL 和 ETCD_ELECTION_TIMEOUT，使用默认值
2. **Milvus 超时**：已设置为 30 秒，解决磁盘慢导致的超时问题
3. **向量化速度**：CPU 模式较慢，逐条处理避免过载
4. **存储路径**：小说目录在 `/workspace/write_novel/novel`
5. **openai_proxy_prod.py**：要由用户运行，在容器内运行，通过 `http://localhost:8001` 访问
6. **Novel RAG 开关**：通过环境变量 `NOVEL_RAG_ENABLED` 控制，默认为 `false`